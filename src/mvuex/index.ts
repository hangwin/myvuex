import { Store, createStore } from './store';
import { storeKey, useStore } from './injectKey';

export default {
    version: '__VERSION__',
    Store,
    createStore,
    storeKey,
    useStore,
}

export {
    Store,
    createStore,
    storeKey,
    useStore,
}